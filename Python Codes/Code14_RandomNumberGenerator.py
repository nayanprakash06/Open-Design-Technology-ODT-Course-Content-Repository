# Import the random module
# This module is used to generate random numbers
import random

# Generate a random integer between 15 and 78 (both values included)
# The generated number is stored in the variable my_r
my_r = random.randint(15, 78)

# Print the randomly generated number on the console
print(my_r)
